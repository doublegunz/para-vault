---
tags: [jurnal]
---

## Fokus hari ini
-

## Catatan / kejadian

referensi dari setup qadrlabs [rclone](obsidian://open?vault=obsidian-vault&file=00-inbox%2Frclone-google-drive-backup-runbook)

```
chown vivida:vivida /home/vivida/.my.cnf
chmod 600 /home/vivida/.my.cnf
```


```
mariadb \
  --defaults-file=/home/vivida/.my.cnf \
  -e "SELECT VERSION();"
```

```
mariadb-dump \
  --defaults-file=/home/vivida/.my.cnf \
  --single-transaction \
  --skip-lock-tables \
  vivida_db \
  > /dev/null
```

```
rclone config \
  --config /home/vivida/.config/rclone/rclone.conf
```


cek
```
python3 - <<'PY'
import configparser

path = "/home/vivida/.config/rclone/rclone.conf"
config = configparser.RawConfigParser()
config.read(path)

client_id = config.get("gdrive", "client_id", fallback="").strip()
client_secret = config.get("gdrive", "client_secret", fallback="").strip()

print("Client ID    :", "CUSTOM" if client_id else "BAWAAN RCLONE")
print("Client secret:", "TERSEDIA" if client_secret else "TIDAK TERSEDIA")
PY
```


workaround supaya otomatis refresh token
```
RCLONE_DRIVE_AUTH_URL='https://accounts.google.com/o/oauth2/auth?prompt=consent' \
rclone authorize "drive" "eyJjbGllbnRfaWQiOiIyNDg5OTE5NTIxOTctZzc1dHVpYzB1anE3MHA1cXMwZGMwdHExcXFvNG80Ym0uYXBwcy5nb29nbGV1c2VyY29udGVudC5jb20iLCJjbGllbnRfc2VjcmV0IjoiR09DU1BYLTFVSWNGTXlSRnVHTDhreXZCNks0YmlnNjRVcGkiLCJzY29wZSI6ImRyaXZlIn0"
```

cek refresh token
```
python3 - <<'PY'
import configparser
import json
import sys

path = "/home/vivida/.config/rclone/rclone.conf"

config = configparser.RawConfigParser()
config.read(path)

try:
    token = json.loads(config["gdrive"]["token"])
except Exception as exc:
    print(f"✘ Gagal membaca token: {exc}")
    sys.exit(1)

for key, label in (
    ("access_token", "Access token"),
    ("refresh_token", "Refresh token"),
    ("expiry", "Expiry"),
):
    value = token.get(key)
    print(
        f"{'✔' if value else '✘'} "
        f"{label} {'tersedia' if value else 'tidak tersedia'}"
    )
PY
```

output:
```
✔ Access token tersedia

✔ Refresh token tersedia

✔ Expiry tersedia
```

buat direktori
```
**vivida@VM-0-14-ubuntu**:**~**$ mkdir backup-system

**vivida@VM-0-14-ubuntu**:**~**$ cd backup-system/

**vivida@VM-0-14-ubuntu**:**~/backup-system**$ mkdir scripts

**vivida@VM-0-14-ubuntu**:**~/backup-system**$ cd ..

**vivida@VM-0-14-ubuntu**:**~**$ mkdir backup

**vivida@VM-0-14-ubuntu**:**~**$ ls

**Project**  **backup**  **backup-system**
```
## Ide & insight
-

## Terhubung ke
- chat backup vivida [session vivida](https://chatgpt.com/c/6a981080-b7b0-83ec-afc1-b6e45239c58c)